# ══════════════════════════════════════════════════════════════
#  AI漫剧智能体工作流 · 版本控制排除项
# ══════════════════════════════════════════════════════════════

# ── 1. 用户素材目录（约 4.3 GB）——不入库 ──────────────────────
# 排除原因（两条都成立）：
#   ① 体积：4.3 GB 中约 1.3 GB 视频 + 700 MB 图片 + 277 MB 安装包，
#      远超 GitHub 单仓库建议上限（1 GB），推送会失败或被拒
#   ② 版权：含第三方课程素材与**院线电影字幕稿**等受版权保护内容，
#      不适合公开仓库
# 说明：素材仅在本地作为「提炼来源」使用，其成果已写入
#       AI漫剧智能体工作流/（见 99-源仓库索引/资料素材全清单.md 的挖掘状态）
data/

# ── 2. IDE 工程数据（本地状态与记忆）——不入库 ─────────────────
# 含 agent 的本地记忆与运行状态，属开发环境私有数据。
#
# ⚠️ 用 `.codebuddy/*` 而不是 `.codebuddy/` —— 后者会把**整个目录**排除，
#    于是下面的 `!` 例外**不可能生效**（git 规定：父目录被排除时，无法再放行其子项）。
.codebuddy/*
#
# ⭐ 例外：**项目级子智能体**是本仓库的**交付物** —— 它们告诉 CodeBuddy
#    「AI漫剧工作流的 7 个流程 agent 怎么用」（规则只读工作流、确定性步骤走 07 的代码）。
#    必须入库才能随仓库共享/迁移。校验：`python 07-智能体运行时/tests/test_agents.py`
!.codebuddy/agents/

# ── 3. 常规忽略 ───────────────────────────────────────────────
__pycache__/
*.py[cod]
.DS_Store
Thumbs.db
*.tmp
*.log
