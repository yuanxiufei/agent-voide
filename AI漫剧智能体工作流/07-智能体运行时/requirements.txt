# AI漫剧资产库Agent · 依赖
#
# ⚠️ 本项目**核心功能零第三方依赖**：
#    · 规则解析（nl_parser）  —— 纯标准库
#    · LLM 客户端（llm_client）—— 纯标准库 urllib
#    · mock 出图（image_provider.MockProvider）—— 纯标准库 zlib + struct 手写 PNG
#    · 资产库/版本/一致性/CLI  —— 纯标准库
#
# 也就是说：**装完 Python 3.9+ 就能直接运行全部流程**（含出图占位图）。
#
# 只有接入**真实图像模型**时才需要下面这个包，且代码里是延迟 import ——
# 不装也不影响 mock 模式。

# 真实图像 API（OpenAI 兼容 / Stability AI）需要：
requests>=2.28.0

# ── 可选（本项目未使用，列出仅供参考）──
# pillow>=10.0.0      # 若以后要做图像合成/预览拼图
# pyyaml>=6.0         # 若要直接读写 ASSET_CARD.yaml（当前 schema.py 已按字段对齐，无需解析 YAML）
