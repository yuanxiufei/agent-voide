# ══════════════════════════════════════════════════════════════
#  行尾统一为 LF
# ══════════════════════════════════════════════════════════════
#
# 为什么必须锁定：
#   本项目部分文件按「hash 锁定」约定**不得改动内容**（如
#   02-服化道/00-主控智能体.md 等规格文件）。Git 在 Windows 上默认
#   会把 LF 转成 CRLF，**这会改变文件字节内容**，导致：
#     ① 锁定约定失效（内容被静默修改）
#     ② 跨平台协作时产生满屏无意义的行尾 diff
#
# 故统一强制 LF，不做平台转换。

* text=auto eol=lf

# 二进制文件不做文本处理
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.mp3  binary
*.mp4  binary
*.zip  binary
*.xlsx binary
*.docx binary
